# Raibert 落足点奖励复盘（2026-08）

## 结论

本次在 Mini Cheetah 原地转向训练中加入的 Raibert 落足点奖励，经实际训练验证产生了负面作用。后续**不要复用或改写本次实现的奖励思路**；清理后也不应将其作为代码模板。

## 本次实现的思路（记录用途，不作为后续方案）

实现位于工作区修改中的 `legged_robot.py`，主要过程如下：

1. 按 `FL`、`FR`、`RL`、`RR` 重排四个足端，避免依赖刚体句柄的返回顺序。
2. 以足端竖直接触力是否超过阈值判断接触、离地和触地事件。
3. 只在平移指令近零、偏航指令非零的原地转向阶段启用该项。
4. 对每条离地腿缓存离地时的机身位置、偏航姿态、速度/偏航指令和时间。
5. 在下一次触地时，根据缓存的离地状态与实际摆动时长，构造包含名义站距、平移前馈和偏航前馈的世界系目标落足点。
6. 将触地时足端水平位置与目标点的二维平方误差求和，并用负权重加入奖励。

实际训练表明，上述“离地状态冻结 + 触地瞬时误差 + 指令驱动目标点”的稠密性和约束方式不适合当前 HIMLoco/Mini Cheetah 训练设置，应避免再次采用。

## 唯一保留的参考数据：默认站立足端间距

后续如需设计**新的、独立验证的**落足相关方法，只可参考默认站立姿态的几何间距：

| 项目 | 数值 |
| --- | ---: |
| 前后足端间距（长度） | `0.38 m` |
| 左右足端间距（宽度） | `0.25 m` |

这些数值对应 Mini Cheetah 默认站立姿态，不代表 Raibert 奖励目标，也不应与本次的摆动时长、接触事件、前馈公式或奖励形式绑定使用。
